kind = "web"
previewPath = "/"
title = "The Liminal"
version = "1.0.0"
id = "artifacts/backrooms-game"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5000

[services.development]
run = "pnpm --filter @workspace/backrooms-game run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/backrooms-game", "run", "build" ]
publicDir = "artifacts/backrooms-game/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5000"
BASE_PATH = "/"
